# Segredos — NUNCA versionar (§7.1)
.env

# Ambiente virtual
.venv/
venv/
__pycache__/
*.pyc

# Dados gerados em runtime — não versionar (§4 / §7.1)
datalake/

# Sistema
.DS_Store

# Workspace pessoal do editor (caminhos locais da maquina, nao do projeto)
*.code-workspace
